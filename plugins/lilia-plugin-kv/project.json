{
  "name": "lilia-plugin-kv",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "plugins/lilia-plugin-kv/src",
  "implicitDependencies": ["lilia-plugin-api"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "cargo build -p lilia-plugin-kv" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "cargo test -p lilia-plugin-kv" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "cargo clippy -p lilia-plugin-kv --all-targets -- -D warnings" } },
    "bench": { "executor": "nx:run-commands", "options": { "command": "cargo bench -p lilia-plugin-kv --no-run" } },
    "package": { "executor": "nx:run-commands", "options": { "command": "cargo build -p lilia-plugin-kv --release" } }
  },
  "tags": ["scope:model", "type:plugin", "lang:rust"]
}
