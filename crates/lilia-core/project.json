{
  "name": "lilia-core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "crates/lilia-core/src",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "cargo build -p lilia-core" } },
    "test": { "executor": "nx:run-commands", "options": { "command": "cargo test -p lilia-core" } },
    "lint": { "executor": "nx:run-commands", "options": { "command": "cargo clippy -p lilia-core --all-targets -- -D warnings" } },
    "bench": { "executor": "nx:run-commands", "options": { "command": "cargo bench -p lilia-core --no-run" } },
    "package": { "executor": "nx:run-commands", "options": { "command": "cargo package -p lilia-core --allow-dirty --no-verify" } }
  },
  "tags": ["scope:engine", "type:library", "lang:rust"]
}
